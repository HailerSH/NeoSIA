package com.dcps.neosia.controller;

import com.dcps.neosia.model.LocalStorage;
import com.dcps.neosia.model.dao.UserRoomDAO;
import com.dcps.neosia.model.pojo.User;
//import com.dcps.neosia.ui.LoginActivity;

public class LoginActivityController {

    private UserRoomDAO userRoomDAO;
   // private LoginActivity loginActivity;

  //  public LoginActivityController(LoginActivity loginActivity) {
    //    this.loginActivity = loginActivity;
      //  this.userRoomDAO = LocalStorage.getLocalStorage(
        //                                                loginActivity.getApplicationContext()
          //                                              ).userRoomDAO();
  //  }


//    public void login(String username, String password) {
//        if (username == null || username.compareTo("") == 0) {
//            loginActivity.messageError("El nombre de usuario es requerido");
//        }
//
//        loginActivity.loginSucced(username, rol);
//    }
}
